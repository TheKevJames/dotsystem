version: 2.1

orbs:
  linter: talkiq/linter@4

executors:
  python:
    docker:
      - image: python:3.13.2
    resource_class: medium

workflows:
  run-jobs:
    jobs:
      - linter/pre-commit:
          executor: python
