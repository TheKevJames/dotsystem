#!/usr/bin/env bash
set -euo pipefail

echo "To connect to an existing cluster:"
echo "  kubectl config use-context \${NAME}"

echo

echo "To authenticate with a new one:"
echo "  gcloud container clusters get-credentials \${NAME} --zone=us-central1-a"

exit 1
