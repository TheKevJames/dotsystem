#!/usr/bin/env bash
set -euo pipefail

echo "To authenticate gcloud with a pre-configured project:"
echo "  gcloud config configurations activate \${PROJECT}"

echo

echo "To grab your auth credentials:"
echo "  export GOOGLE_APPLICATION_CREDENTIALS=${GCLOUD_CREDS_DIR}/\${PROJ}.json"

exit 1
