#!/usr/bin/env bash
set -euo pipefail
VENV="${XDG_DATA_HOME}/venv/getchanges"

[[ -d "${VENV}" ]] || python3 -m venv "${VENV}"
cd "${VENV}"
source ./bin/activate

[[ -f ./bin/changes ]] || python3 -m pip install getchanges
./bin/changes "$@"
