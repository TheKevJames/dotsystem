#!/usr/bin/env bash
set -euo pipefail
VENV="${XDG_DATA_HOME}/venv/twine"

[[ -d "${VENV}" ]] || python3 -m venv "${VENV}"
cd "${VENV}"
source ./bin/activate

[[ -f ./bin/twine ]] || python3 -m pip install twine
./bin/twine "$@"
