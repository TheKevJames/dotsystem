#!/usr/bin/env bash
set -euo pipefail
VENV="${XDG_DATA_HOME}/venv/catt"

[[ -d "${VENV}" ]] || python3 -m venv "${VENV}"
cd "${VENV}"
source ./bin/activate

[[ -f ./bin/catt ]] || python3 -m pip install catt
./bin/catt "$@"
