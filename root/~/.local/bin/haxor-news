#!/usr/bin/env bash
set -euo pipefail
VENV="${XDG_DATA_HOME}/venv/haxor-news"

[[ -d "${VENV}" ]] || python3 -m venv "${VENV}"
cd "${VENV}"
source ./bin/activate

[[ -f ./bin/haxor-news ]] || python3 -m pip install haxor-news
./bin/haxor-news "$@"
