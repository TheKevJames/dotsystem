#!/usr/bin/env bash
set -euo pipefail
VENV="${XDG_DATA_HOME}/venv/bugwarrior"

[[ -d "${VENV}" ]] || python3 -m venv "${VENV}"
cd "${VENV}"
source ./bin/activate

[[ -f ./bin/bugwarrior-pull ]] || python3 -m pip install "bugwarrior[jira]"
./bin/bugwarrior-pull "$@"
