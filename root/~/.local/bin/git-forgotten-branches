#!/usr/bin/env bash
set -euo pipefail

for folder in $(fd -td -d1 .); do
  branches=$(git --git-dir "./${folder}/.git" branch -a | grep -E '(kj|kjames)/' ||:)
  if [ ! -z "${branches:-}" ]; then
    echo "${folder}"
    echo "${branches}"
    echo
  fi
done
