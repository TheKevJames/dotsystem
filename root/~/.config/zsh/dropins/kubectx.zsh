# TODO: have to zcompinit this rather than source it
# https://github.com/ahmetb/kubectx#linux
## source "${XDG_SRC_HOME}/kubectx/completion/kubectx.zsh"
## source "${XDG_SRC_HOME}/kubectx/completion/kubens.zsh"

export PATH="${XDG_SRC_HOME}/kubectx:${PATH}"
